# Sample PDFs

Place sample PDFs here for the demo host (Phase 6).

For **Phase 3 linearization** testing, include at least one **linearized** PDF (100+ pages recommended). When served over HTTP with `Accept-Ranges: bytes`, the viewer issues `Range` requests (`206 Partial Content`) while scrolling.

Vite’s dev server supports byte ranges for static files in `public/`.
